{
  "name": "parcel-plugin-pwa-manifest",
  "version": "2.2.0",
  "description": "A Parcel plugin that generates a Web App Manifest, creates all necessary icons, and more!",
  "main": "lib/index.js",
  "author": "Arjun Barrett",
  "license": "MIT",
  "keywords": [
    "pwa",
    "parcel-bundler",
    "parcel",
    "manifest",
    "webmanifest",
    "favicon",
    "icon"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/101arrowz/parcel-plugin-pwa-manifest.git"
  },
  "scripts": {
    "prettify": "prettier --single-quote --trailing-comma none --write index.ts",
    "build": "yarn prettify && tsc",
    "prepack": "yarn build",
    "preversion": "yarn prettify"
  },
  "dependencies": {
    "@parcel/logger": "<=1.11.1",
    "sharp": "^0.23.2"
  },
  "peerDependencies": {
    "parcel-bundler": ">=1.9"
  },
  "devDependencies": {
    "@types/node": "^12.12.6",
    "@types/parcel-bundler": "^1.12.1",
    "@types/sharp": "^0.23.0",
    "prettier": "^1.18.2"
  }
}
